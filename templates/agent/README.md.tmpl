# Welcome to s3

This service provides a Docker-managed S3 instance for caching and data storage:

- Provides connection string as configuration to dependent services
- Uses the official S3 7 Alpine Docker image
- Supports optional password authentication

This service provides a local S3 instance for development and testing purposes.
